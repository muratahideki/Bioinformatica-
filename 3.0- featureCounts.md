
```bash
featureCounts \
  -a anotacao.gtf \       # arquivo de anotação da cana-de-açúcar
  -o counts.txt \         # saída: tabela de contagem
  -p \                    # indica paired-end
  -t exon \               # conta apenas reads que caem em exons
  -g gene_id \            # agrupa pelas IDs dos genes
  -s 0 \                  # 0 = dados não strand-specific; use 1 ou 2 se suas leituras forem strand-specific
  arquivo_sorted.bam      # arquivo BAM de entrada
```
